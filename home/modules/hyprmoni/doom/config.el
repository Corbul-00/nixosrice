(setq doom-theme 'doom-one)

(setq doom-font
      (font-spec
       :family "JetBrainsMono Nerd Font"
       :size 12))

(setq display-line-numbers-type 'relative)

(setq fancy-splash-image
      (expand-file-name "emacsbg.png" doom-private-dir))
